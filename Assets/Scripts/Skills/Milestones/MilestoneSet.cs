namespace TimelessEchoes.Skills
{
    /// <summary>
    /// Identifies milestone set membership. Designers should keep <see cref="None"/> as the first value.
    /// </summary>
    public enum MilestoneSet
    {
        None = 0,
        // Add additional set names here (e.g., CarlsBlessing, WanderersGrace, etc.).
    }
}
